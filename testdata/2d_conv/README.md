# 2D conv

This is a custom-made neural network featuring 2-dimensional convolutional layers, specifically crafted to stress test ZK proving with complex ONNX operators.

To generate the necessary files (`network.onnx` and `input.json`) for this model, run:

```bash
python3 gen.py
```
